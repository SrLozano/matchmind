# Shared Package Space

Use this directory later for code that both apps need, such as generated API clients, shared TypeScript types, or product constants.

Leave it empty until there is a real cross-app dependency.
